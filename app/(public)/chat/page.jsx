import ChatContent from "./ChatContent";

export const metadata = {
  title: "Customer Support Chat | PRT Store",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ChatPage() {
  return <ChatContent />;
}